# Ultimate Todo List

##### Local Installation

    - yarn install
    - yarn start


##### Docker Build
    - yarn build
    - docker build . -t mytodo
    - docker run -p 3000:80 -d mytodo
    - Open in the browser:

    http://localhost:3000